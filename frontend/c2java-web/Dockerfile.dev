# ============================================
# C2JAVA Frontend Dockerfile (개발용)
# Hot Reload 지원
# ============================================

FROM node:20-alpine

WORKDIR /app

# 패키지 파일 복사
COPY package*.json ./

# 의존성 설치
RUN npm ci

# 소스 코드 복사
COPY . .

EXPOSE 3000

# 개발 서버 실행 (HMR 지원)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
